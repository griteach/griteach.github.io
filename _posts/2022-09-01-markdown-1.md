---
title: "마크다운 익히기 #1"
categories:
 - TIL

tags:
 - markdown

---

# 마크다운 정리

> 제목은 #1 ~ #6까지 사용한다.

```javascript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```
위의 코드 입력 때문에 다 갈아엎기로 했다. 참. 비효율적이야.
